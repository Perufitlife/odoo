<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_choose_agency_wizard_form" model="ir.ui.view">
        <field name="name">choose.agency.wizard.form</field>
        <field name="model">choose.agency.wizard</field>
        <field name="arch" type="xml">
            <form string="Elegir Agencia de Envío">
                <group>
                    <field name="sale_id" invisible="1"/>
                    <field name="agency_type" widget="radio"/>
                </group>
                <footer>
                    <button string="Siguiente"
                            name="action_confirm_agency"
                            type="object"
                            class="btn-primary"/>
                    <button string="Cancelar"
                            special="cancel"
                            class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>